# import the board compatible string once and propagate it to all devices
# so it can be matched in other rules
ACTION=="add", ENV{OF_COMPATIBLE_0}=="simple-bus", IMPORT{program}="dt-compatible"
ACTION=="add", IMPORT{parent}="BASE_COMPATIBLE"
